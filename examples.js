module.exports = {
	controller: {
		// uses console.log and some basic formatting to output "help" information on what is known about the
		// controlling command. this is an example of how help pages could be generated.
		help: require('./examples/controller.help')
	}
};